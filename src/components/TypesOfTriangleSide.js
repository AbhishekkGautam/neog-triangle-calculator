import React from "react";

const TypesOfTriangleSide = ({ isShow }) => {
  return (
    <div style={{ display: `${isShow}` }}>
      <h1>Types of triangle - side</h1>
    </div>
  );
};

export default TypesOfTriangleSide;
