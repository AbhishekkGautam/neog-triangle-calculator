import React from "react";

const TypesOfTriangleAngle = ({ isShow }) => {
  return (
    <div style={{ display: `${isShow}` }}>
      <h1>Types of triangle - angle</h1>
    </div>
  );
};

export default TypesOfTriangleAngle;
